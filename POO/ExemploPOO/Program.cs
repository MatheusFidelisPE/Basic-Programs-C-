using System;
using ExemploPOO.Helper;
using ExemploPOO.Models;
namespace ExemploPOO
{
    class Program
    {

        static void Main(string[] args)
        {
            
            // Pessoa p1 = new Pessoa();
            // p1.Nome = "Bob";
            // p1.Idade = 20;

            // p1.Apresentar();

            // Retangulo r1 = new Retangulo();

            // r1.DefinirMedidas(-2, 0);
            // Console.WriteLine(r1.area());

            // Pessoa p1 = new Professor();
            // Professor prof = new Professor();

            // Pessoa p2 = new Aluno();
            // Aluno alu = new Aluno();

            // p1.Nome = "Professor chato";
            // p1.Idade = 43;
            

            // prof.Idade = 54;
            // prof.Nome = "Novo prof";
            // prof.Salario = 2500;

            // prof.Apresentar();

            // alu.Nome = "Matheus";
            // alu.Nota = 10;
            // alu.Apresentar();

            // Calculadora calc = new Calculadora();

            // Console.WriteLine(calc.Somar(10,5));
            // System.Console.WriteLine(calc.Somar(10,2,4));

            // Corrente c = new Corrente();
            // c.Creditar(10);
            // c.Creditar(10);
            // c.ExibirSaldo();
            // Computador comp = new Computador();
            // System.Console.WriteLine(comp.ToString());

            // FileHelper fp = new FileHelper();
            // string caminho = "D:\\trabalhando_com_arquivos"; 
            // var pathCombine = Path.Combine(caminho,"pasta_teste4","subPastaTeste3");
            // var pathCombine2 = Path.Combine(caminho,"pasta teste 3");
            // var pathCriarTexto = Path.Combine(caminho,"pastaTeste1","subPastaTeste1");
            // pathCriarTexto = Path.Combine(pathCriarTexto,"textoTeste1.txt");
            // string conteudo = "Estou escrevendo no arquivo de texto";
            // List<string> lista = new List<string> {"linha 1", "linha 2", "linha 3"};
            // fp.ListarDiretorios(caminho);
            // fp.ListarArquivosDiretorio(caminho);
            // fp.CriarDiretorio(pathCombine);
            // fp.ApagarDiretorio(pathCombine2);
            // fp.criarArquivoTexto(pathCriarTexto,conteudo);
            // fp.CriarArquivoTextoStream(pathCriarTexto, lista);
            // fp.EditarArquivoTexto(pathCriarTexto,"\n estou adicionando novas linhas no texto final.");
            // fp.LerArquivo(pathCriarTexto);
            // fp.LerArquivoStream(pathCriarTexto);
            // string novoArquivo = Path.Combine(caminho,"pastaTeste2","ArquivoComNomeNovo.txt");
            // fp.MoverArquivo(pathCriarTexto,novoArquivo);
            // fp.CopiarArquivo(novoArquivo,pathCriarTexto);
            // fp.DeletarArquivo(pathCriarTexto);


        }
    }
}